import { withInstall } from '@ui/with-install'
import Pagination from './index.vue'

/** d-pagination 分页 */
export const DPagination = withInstall(Pagination, 'DPagination')

export default DPagination
