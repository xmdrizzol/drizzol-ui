import { withInstall } from '@ui/with-install'
import Progress from './index.vue'

/** d-progress 进度条（upload-notify 内部同款） */
export const DProgress = withInstall(Progress, 'DProgress')

export default DProgress
