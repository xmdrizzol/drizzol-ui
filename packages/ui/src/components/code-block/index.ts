import { withInstall } from '@ui/with-install'
import CodeBlock from './index.vue'

/** d-code-block 代码块（语言标识 + 复制按钮，深色高亮风格） */
export const DCodeBlock = withInstall(CodeBlock, 'DCodeBlock')

export default DCodeBlock
